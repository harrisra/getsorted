# Copy to .env for docker-compose. Only needed once real credentials exist —
# the app runs fine without them until Google login / Populate is exercised.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
PEPESTO_API_KEY=
