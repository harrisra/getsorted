# Copy to .env for docker-compose. Only needed once real Google OAuth
# credentials exist — the app runs fine without them until Google login
# is actually exercised.
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
