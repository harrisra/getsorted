VITE_API_BASE_URL=http://localhost:8000
# The Google OAuth client's ID (not the secret — this one is public, baked
# into the frontend bundle). Leave unset to hide the "Continue with Google"
# button, e.g. while the OAuth client hasn't been created yet.
VITE_GOOGLE_OAUTH_CLIENT_ID=
